# State is local by design (see the commented backend in main.tf) and holds the
# generated JWT signing key and the RDS master password in plaintext.
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
crash.log

# .terraform.lock.hcl is deliberately not ignored: it pins provider versions, so
# committing it is what makes two applies resolve the same way.
