/**
 * Prompts and response schemas for everything we ask an LLM.
 *
 * The root export is the current version. Import from
 * `@cv-jobs-compatibility/prompt-schemas/v1` instead to pin one explicitly -
 * which is what a consumer re-reading an old extraction should do.
 */

export * from './v1'
